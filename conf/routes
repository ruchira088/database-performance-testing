# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /service-info       web.controllers.InfoController.serviceInfo()

POST    /tiny-url           web.controllers.UrlController.createTinyUrl()

GET     /tiny-url/:key      web.controllers.UrlController.fetchUrl(key)